package template;

public class BinaryOperationTemplate {

    public void operation(){

        // 基本操作
        // a = 0 ^ a = a ^ 0
        // 0 = a ^ a;

        // 交换两个数
        // a = a ^ b
        // b = a ^ b
        // a = a ^ b

        // 移除最后一个1
        // a = n & (n - 1)

        // 获取最后一个1
        // diff = (n & (n - 1)) ^ n
    }

}
